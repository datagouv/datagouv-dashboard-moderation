{
  "name": "datagouv-dashboard-moderation",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm install --production false && npm run build && npm install --production true"
  },
  "engines": {
    "node": "13.x",
    "yarn": "1.x"
  }
}
