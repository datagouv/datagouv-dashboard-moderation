{
  "name": "dokku-dgf-dashboard-moderation",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm install --cwd frontend --production false && npm --cwd frontend run build && npm install --cwd frontend --production true"
  },
  "engines": {
    "node": "13.x",
    "npm": "6.14.x"
  }
}
